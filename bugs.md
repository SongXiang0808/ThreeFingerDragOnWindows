# 记录一下在使用过程中的还存在的一些问题以及可能的解决方法思路

## 获取触摸板尺寸
能否在这个程序中加上获取触摸板尺寸及相关的触摸板信息的相关功能，这方面也可以参考F:\TouchPadCode\MouseLikeTouchPad_I2C\SRC\MouseLikeTouchPad_I2C.cpp这个文件中的相关实现，这些信息会对下面消除误触有很大的作用。

## 左键点击
左键操作有时不太灵敏，重点表现在因为人的手指的食指比人的中指要短，所以在使用触摸板的时候，在手指舒适状态的时候就会呈现出一种在触摸的时候，左边的手指一般会比中间的滑动指要靠下一些。
## 左键拖动
拖动的时候，在两个手指距离比较近的时候，会有问题，拖不动，但是当两个手指距离比较远的时候，可以拖动，但是触发的几率不好判断。
## 双指滑动
因为在原生的触摸板驱动中有着双指滑动的功能，但是在现在的程序加持下，在双指滑动的时候，有很大的概率触发右键的功能，有一定的概率触发左键的功能。
## 三指点击
在原生的触摸板驱动中具有三指点击为中键的功能，在现在的程序中，也是有很大的概率会触发右键的功能，这导致了在一些场景中由于触发了右键而吃掉了原声驱动的中键功能。

## 如何解决
你可以自己先尝试基于一些手指的特征，手指点击时的特征等一系列的来做一定的曲风；另一方面，你可以学习F:\TouchPadCode\MouseLikeTouchPad_I2C\SRC\MouseLikeTouchPad_I2C.cpp这个文件中的相关的实现方法，但是要注意两者的距离单位可能不同，此外F:\TouchPadCode\MouseLikeTouchPad_I2C\SRC\MouseLikeTouchPad_I2C.h这个头文件中的定义的相关变量和参数
